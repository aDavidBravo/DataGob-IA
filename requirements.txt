# ============================================================
# DataGob-IA — requirements.txt
# Compatible con Python 3.12 + Windows/Linux/Mac
# Instalar en orden: pip install -r requirements.txt
# ============================================================

# ---- Core Data Science ----------------------------------------
numpy==1.26.4
pandas==2.2.1
scikit-learn==1.4.2
xgboost==2.0.3
lightgbm==4.3.0

# ---- NLP (pesados ~2GB — omitir si no usas titulo_verifier.py)
# sentence-transformers==2.7.0
# transformers==4.40.0
# torch==2.3.0
# spacy==3.7.4
# Para instalar NLP manualmente:
#   pip install torch==2.3.0
#   pip install sentence-transformers==2.7.0

# ---- API (liviano, instalar siempre) --------------------------
fastapi==0.110.3
uvicorn[standard]==0.29.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9
pydantic==2.7.1
pydantic-settings==2.2.1

# ---- Database -------------------------------------------------
sqlalchemy==2.0.29
alembic==1.13.1
redis==5.0.4
# psycopg2-binary requiere Docker corriendo:
psycopg2-binary==2.9.9

# ---- Data Engineering (opcional — requiere Java para Spark) ---
# pyspark==3.5.1          # Requiere Java 11/17 instalado
# delta-spark==3.1.0      # Requiere pyspark
# dbt-core==1.8.0         # Instalar solo en entorno Linux/WSL
# dbt-postgres==1.8.0

# ---- Data Quality ---------------------------------------------
pandera==0.19.3
# great-expectations tiene conflictos en Python 3.12, instalar así:
# pip install great-expectations==0.18.15 --use-deprecated=legacy-resolver

# ---- Record Linkage / Deduplicación ---------------------------
fuzzywuzzy==0.18.0
python-levenshtein==0.25.1
jellyfish==1.0.3
# recordlinkage==0.16  # Tiene dep. de numpy estricta, instalar aparte si falla

# ---- Visualización --------------------------------------------
matplotlib==3.8.4
seaborn==0.13.2
plotly==5.22.0

# ---- Seguridad (hashlib viene incluido en Python stdlib) ------
cryptography==42.0.5

# ---- Testing --------------------------------------------------
pytest==8.2.0
pytest-cov==5.0.0
httpx==0.27.0

# ---- Utilidades -----------------------------------------------
python-dotenv==1.0.1
loguru==0.7.2
tqdm==4.66.2
PyYAML==6.0.1
typer==0.12.3
faker==24.11.0

# ---- Jupyter (opcional) ---------------------------------------
# jupyter==1.0.0
# ipykernel==6.29.4
